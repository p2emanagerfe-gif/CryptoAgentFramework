# Copy to .env and fill in real values. .env is gitignored — never commit it,
# never paste real values into a chat/agent session.

# One env var per wallet referenced in wallets.json, e.g.:
PK_TREASURY_MINT_01=
PK_TREASURY_MINT_02=

# Optional: override RPC URLs defined per-target in approved-mints.json
# ABSTRACT_RPC_URL=https://api.mainnet.abs.xyz
